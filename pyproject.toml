[project]
name = "calculator_package"
version = "0.2.0"
description = "A reusable Python calculator package for learning GitHub collaboration."
authors = [
    { name = "Workshop Team" }
]
readme = "README.md"
requires-python = ">=3.8"

[tool.pytest.ini_options]
pythonpath = [
    "."
]
